# ============================================================
#  NeuroGallery 环境变量示例
# ============================================================
#
#  重要变更：画架的「在线生成画作」已改为【后端代理转发】。
#  前端 bundle 中不再包含任何 API key。
#
#  —— 后端 key（必填，用于 server/index.js）——
#  复制到 server/.env（已被 .gitignore 忽略，不会提交）：
#      AGNES_API_KEY=sk-xxxx
#
#  —— 后端配置（可选，server/config.json，可被提交）——
#      baseURL    : OpenAI 兼容网关地址
#      imageModel : 图像生成模型名
#
#  —— 前端配置（可选，public/textures/paintings/easel_config.json）——
#      apiBase    : 后端地址，默认 "/api"（同源）；部署独立后端时改成其 URL
#      api.baseURL / api.imageModel : 仅作展示说明，实际以 server 端为准
#
#  本地运行：  npm run dev   （会自动拉起后端 server/index.js）
#  公网生成：  需把 server/ 部署到支持 Node 的平台，再把 apiBase 指向它。
